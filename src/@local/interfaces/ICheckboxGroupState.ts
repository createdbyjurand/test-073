export interface ICheckbox {
  all: {
    checked: boolean | 'indeterminate';
  };
  cond1: {
    checked: boolean;
  };
  cond2: {
    checked: boolean;
  };
  cond3: {
    checked: boolean;
  };
  cond4: {
    checked: boolean;
  };
}
